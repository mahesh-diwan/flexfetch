/target
# Cargo.lock is committed: this is a binary workspace (release/CI/docker use
# `--locked` for reproducible builds — the Dockerfile COPY . . needs it).
# mdBook build output (book/book.toml sets build-dir = target)
book/target
# Generated site output (the Docs & Site workflow builds the book into
# site/docs before deploying — regenerate locally with:
#   mdbook build book --dest-dir site/docs
site/docs

# AI tooling / agent working state (not project files)
.claude/
.opencode/
.playwright/
.playwright-cli/
.superpowers/
.memory/
.ruff_cache/

# Internal / non-user-facing (removed from tree, kept in history)
docs/superpowers/
fuzz/
scripts/subagent-driven-development/
scripts/gen_fastfetch_logos.py
scripts/pgo.sh
scripts/site_hero.py
scripts/verify-repro.sh
cliff.toml
packaging/
tests/fixtures/
flexfetch-core/create_logos.py
doc/plugins.md
doc/templates.md
schemas/
