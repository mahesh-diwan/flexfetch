/target
# Cargo.lock is committed: this is a binary workspace (release/CI use
# `--locked` for reproducible builds).
# mdBook build output (book/book.toml sets build-dir = target)
book/target
# Generated site output (the Docs & Site workflow builds the book into
# site/docs before deploying — regenerate locally with:
#   mdbook build book --dest-dir site/docs
site/docs

# AI tooling / agent working state (not project files)
.claude/
.opencode/
.playwright/
.playwright-cli/
.playwright-mcp/
.superpowers/
.memory/
.ruff_cache/

# AI planning / design artifacts (removed from the repo)
.impeccable/
animation-plans/
docs/superpowers/
flexfetch.md

# Astro prototype site (removed — the repo ships a single static site)
site/DESIGN.md
site/animation-plans/
site/astro.config.mjs
site/modules.html
site/node_modules/
site/dist/
site/.astro/
site/src/
site/public/
site/package.json
site/package-lock.json
site/tsconfig.json

# Local screenshots / scratch files
/*.png
site/*.png
*.cjs
book/src/*#*

# mdBook anchor junk (scraper artifacts)
book/src/*#*
