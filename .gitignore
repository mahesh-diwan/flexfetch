/target
# Cargo.lock is committed: this is a binary workspace (release/CI/docker use
# `--locked` for reproducible builds — the Dockerfile COPY . . needs it).
# mdBook build output (book/book.toml sets build-dir = target)
book/target
