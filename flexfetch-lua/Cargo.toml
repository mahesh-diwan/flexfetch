[package]
name = "flexfetch-lua"
version.workspace = true
edition.workspace = true

[dependencies]
flexfetch-core = { path = "../flexfetch-core" }
# vendored: bundles Lua 5.4 from source so builds need no system Lua headers
mlua = { version = "0.10", features = ["lua54", "vendored"] }
